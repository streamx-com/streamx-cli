name: 'Build and release if tag found'

# Runs on pull requests, pushes to main, tag pushes (which trigger releases) and on pull request reviews.
on:
  pull_request:
  pull_request_review:
    types: [ submitted ]
  push:
    branches:
      - main
    tags:
      - '*'

# One run at a time per branch; all releases share a single queue so tags can't race.
concurrency:
  group: >-
    ${{
      startsWith(github.ref, 'refs/tags/')
        && (
          contains(github.ref, '-rc')
            && format('{0}-release-rc', github.workflow)
            || format('{0}-release-stable', github.workflow)
        )
        || format('{0}-{1}', github.workflow, github.ref)
    }}

# Shown in GitHub Actions UI to distinguish build types:
# - Regular builds
# - Preview releases
# - Releases
run-name: >-
  ${{
    startsWith(github.ref, 'refs/tags/') && contains(github.ref_name, '-rc.')
      && format('Preview release {0}', github.ref_name)
    || startsWith(github.ref, 'refs/tags/')
      && format('Release {0}', github.ref_name)
    || format('Build {0} - {1}', github.head_ref || github.ref_name,
         github.event.pull_request.head.sha || github.sha)
  }}

permissions:
  id-token: write
  checks: write
  contents: write

jobs:
  setup:
    # pull_request_review triggers on any review (approved, changes requested, comment).
    # Skip non-approval reviews to avoid redundant builds.
    if: >-
      github.event_name != 'pull_request_review'
      || github.event.review.state == 'approved'
    runs-on: ubuntu-24.04
    name: setup
    outputs:
      platforms: ${{ steps.matrix.outputs.platforms }}
      build_native: ${{ steps.flags.outputs.build_native }}
    steps:
      - name: Define platform matrix
        id: matrix
        run: |
          PLATFORMS='[
            { "os": "ubuntu-24.04",     "name": "linux-amd64",  "artifact": "streamx-linux-x86_64" },
            { "os": "ubuntu-24.04-arm", "name": "linux-arm64",  "artifact": "streamx-linux-aarch64" },
            { "os": "macos-26",         "name": "macos-arm64",  "artifact": "streamx-macos-aarch64" },
            { "os": "macos-26-intel",   "name": "macos-amd64",  "artifact": "streamx-macos-x86_64" }
          ]'
          echo "platforms=$(echo "$PLATFORMS" | jq -c .)" >> "$GITHUB_OUTPUT"

      - name: Determine build flags
        id: flags
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BUILD_NATIVE=false
          
          # Always build native binaries on tags and main branch pushes
          if [[ "${{ github.ref }}" == refs/tags/* ]] \
            || [[ "${{ github.event_name }}" == "push" && "${{ github.ref }}" == "refs/heads/main" ]]; then
            BUILD_NATIVE=true
          fi
          
          # For any PR-related event, check if the PR already has an approval
          if [[ "$BUILD_NATIVE" == "false" && -n "${{ github.event.pull_request.number }}" ]]; then
            APPROVED=$(gh pr view ${{ github.event.pull_request.number }} \
              --repo ${{ github.repository }} \
              --json reviewDecision --jq '.reviewDecision')
            if [[ "$APPROVED" == "APPROVED" ]]; then
              BUILD_NATIVE=true
            fi
          fi
          
          echo "build_native=$BUILD_NATIVE" >> "$GITHUB_OUTPUT"
          echo "Build native: $BUILD_NATIVE"

  build-jar:
    needs: setup
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-env
        with:
          gcp_workload_identity_provider: ${{ secrets.GCP_STREAMX_RELEASES_WORKLOAD_IDENTITY_PROVIDER }}
          gcp_service_account: ${{ secrets.GCP_STREAMX_RELEASES_RELEASE_DEPLOY_SA }}
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run tests with native-image-agent
        run: |
          .github/scripts/with-capture-docker-logs.sh ./mvnw verify -Pci

      - name: Upload Docker logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: docker-logs-${{ github.job }}
          path: target/docker-logs/
          if-no-files-found: ignore

      - name: Upload test reports
        if: always()
        uses: scacap/action-surefire-report@v1
        with:
          check_name: Test report - generate-reachability-metadata

      # GraalVM native image requires reachability metadata (reflection configs, etc.) generated by the tracing agent during tests.
      # This can't be produced via static analysis - the agent records reached classes at test runtime to META-INF/native-image.
      #
      # macOS runners lack Docker support needed for our tests.
      # Intel mac has it via QEMU, but it's too slow - streamx-runner fails to start by timeouts.
      # Since these configs are cross-platform, we generate them on Linux and share across matrix builds.
      #
      # More detailed explanation: https://github.com/streamx-com/streamx-cli/pull/13#discussion_r2882693570
      - name: Upload reachability metadata
        uses: actions/upload-artifact@v4
        with:
          name: reachability-metadata
          path: src/main/resources/META-INF/native-image/

      - name: Upload runner JAR
        uses: actions/upload-artifact@v4
        with:
          name: streamx-runner-jar
          path: target/*-runner.jar

  build-native:
    if: needs.setup.outputs.build_native == 'true'
    needs: [setup, build-jar]
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.platforms) }}
    runs-on: ${{ matrix.os }}
    name: ${{ matrix.name }}-build
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-env
        with:
          gcp_workload_identity_provider: ${{ secrets.GCP_STREAMX_RELEASES_WORKLOAD_IDENTITY_PROVIDER }}
          gcp_service_account: ${{ secrets.GCP_STREAMX_RELEASES_RELEASE_DEPLOY_SA }}
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download reachability metadata
        uses: actions/download-artifact@v4
        with:
          name: reachability-metadata
          path: src/main/resources/META-INF/native-image/

      - name: Build native-image executable
        run: |
          .github/scripts/with-capture-docker-logs.sh ./mvnw verify -Pci,native

      - name: Upload test reports
        if: always()
        uses: scacap/action-surefire-report@v1
        with:
          check_name: Test report - build ${{ matrix.name }}

      - name: Upload Docker logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: docker-logs-${{ matrix.name }}
          path: target/docker-logs/
          if-no-files-found: ignore

      - name: Prepare artifact
        run: |
          mkdir -p staging
          cp target/*-runner staging/${{ matrix.artifact }}
          chmod +x staging/${{ matrix.artifact }}

      - name: Smoke test
        run: ./staging/${{ matrix.artifact }} --help

      - name: Upload native executable
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: staging/${{ matrix.artifact }}

  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build-native
    runs-on: ubuntu-24.04
    outputs:
      brew_tap_slug: ${{ steps.release-type.outputs.brew_tap_slug }}
      release_repo: ${{ steps.release-type.outputs.release_repo }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-tags: true
      - uses: ./.github/actions/setup-env
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download native binary artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: streamx-*
          merge-multiple: true

      - name: Download JAR artifacts
        uses: actions/download-artifact@v4
        with:
          name: streamx-runner-jar
          path: target/

      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.GH_STREAMX_RELEASE_APP_ID }}
          private-key: ${{ secrets.GH_STREAMX_RELEASE_APP_PRIVATE_KEY }}
          owner: streamx-com
          repositories: homebrew-tap,homebrew-preview-tap,streamx-cli,streamx-cli-preview

      - name: Determine release type
        id: release-type
        run: |
          TAG="${{ github.ref_name }}"
          if [[ "$TAG" == *-rc.* ]]; then
            echo "type=preview" >> $GITHUB_OUTPUT
            echo "jreleaser_config=.github/jreleaser/jreleaser-preview.yml" >> $GITHUB_OUTPUT
            echo "brew_tap=homebrew-preview-tap" >> $GITHUB_OUTPUT
            echo "brew_tap_slug=streamx-com/preview-tap" >> $GITHUB_OUTPUT
            echo "release_repo=streamx-com/streamx-cli-preview" >> $GITHUB_OUTPUT
          else
            echo "type=stable" >> $GITHUB_OUTPUT
            echo "jreleaser_config=.github/jreleaser/jreleaser.yml" >> $GITHUB_OUTPUT
            echo "brew_tap=homebrew-tap" >> $GITHUB_OUTPUT
            echo "brew_tap_slug=streamx-com/tap" >> $GITHUB_OUTPUT
            echo "release_repo=streamx-com/streamx-cli" >> $GITHUB_OUTPUT
          fi
          echo "Release type: $(grep type <<< "$(cat $GITHUB_OUTPUT)" | head -1)"

      - name: Release with JReleaser
        env:
          JRELEASER_GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          JRELEASER_HOMEBREW_GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          chmod +x artifacts/streamx-*
          
          echo "Pre-create platform ZIPs for native binaries"
          VERSION=$(./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout)
          for PLATFORM in linux-x86_64 linux-aarch64 macos-aarch64 macos-x86_64; do
            mkdir -p staging/$PLATFORM/bin
            cp artifacts/streamx-$PLATFORM staging/$PLATFORM/bin/streamx
            cp LICENSE.txt staging/$PLATFORM/
            cd staging
            zip -r ../artifacts/streamx-${VERSION}-${PLATFORM}.zip $PLATFORM/
            cd ..
          done
          
          echo "Prepare Java binary JAR"
          cp target/*-runner.jar artifacts/streamx-jar.jar

          ./mvnw jreleaser:assemble jreleaser:full-release \
            -Djreleaser.config.file=${{ steps.release-type.outputs.jreleaser_config }} \
            -Djreleaser.strict=true

      - name: Upload JReleaser output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jreleaser-output
          path: |
            target/jreleaser/trace.log
            target/jreleaser/output.properties

      # Homebrew doesn't support multiple versions of the same formula.
      # Because of this, we create a separate formula for each version.
      - name: Create versioned Homebrew formula
        env:
          HOMEBREW_GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          BREW_TAP: ${{ steps.release-type.outputs.brew_tap }}
        run: |
          git clone https://x-access-token:${HOMEBREW_GITHUB_TOKEN}@github.com/streamx-com/${BREW_TAP}.git
          cd ${BREW_TAP}/Formula
          
          VERSION=$(cd ../../ && ./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout)
          FILENAME="streamx@${VERSION}.rb"
          
          if [ -f "$FILENAME" ]; then
            echo "Versioned formula already exists, skipping."
            exit 0
          fi
          
          echo "Copy the freshly-pushed unversioned formula"
          cp streamx.rb "$FILENAME"
          
          CLASS_SUFFIX=$(echo "$VERSION" | sed -E 's/[^a-zA-Z0-9]+([a-zA-Z])/\U\1/g; s/[^a-zA-Z0-9]//g')
          sed -i "s/class Streamx/class StreamxAT${CLASS_SUFFIX}/" "$FILENAME"
          
          git config user.name "streamx-cli-release-bot[bot]"
          git config user.email "264353404+streamx-cli-release-bot[bot]@users.noreply.github.com"
          git add "$FILENAME"
          git commit -m "Add versioned formula for ${VERSION}"
          git push

  release-test:
    needs: [setup, release]
    name: ${{ matrix.name }}-release-test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.platforms) }}
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-env
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.GH_STREAMX_RELEASE_APP_ID }}
          private-key: ${{ secrets.GH_STREAMX_RELEASE_APP_PRIVATE_KEY }}
          owner: streamx-com
          repositories: homebrew-tap,homebrew-preview-tap,streamx-cli,streamx-cli-preview

      - name: Test Homebrew install
        run: |
          echo "Test latest version"
          brew install ${{ needs.release.outputs.brew_tap_slug }}/streamx
          streamx --version | grep -q "${{ github.ref_name }}"
          brew uninstall streamx

          echo "Test versioned formula"
          brew install ${{ needs.release.outputs.brew_tap_slug }}/streamx@${{ github.ref_name }}
          streamx --version | grep -q "${{ github.ref_name }}"
          brew uninstall streamx

      - name: Test native binary from GitHub release
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          gh release download "${{ github.ref_name }}" \
            --repo "${{ needs.release.outputs.release_repo }}" \
            --pattern "${{ matrix.artifact }}" \
            --dir .
          chmod +x ${{ matrix.artifact }}
          ./${{ matrix.artifact }} --version | grep -q "${{ github.ref_name }}"

      - name: Test native archive from GitHub release
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          ARCHIVE_PLATFORM=$(echo "${{ matrix.artifact }}" | sed 's/^streamx-//')
          gh release download "${{ github.ref_name }}" \
            --repo "${{ needs.release.outputs.release_repo }}" \
            --pattern "streamx-*-${ARCHIVE_PLATFORM}.zip" \
            --dir .
          unzip streamx-*-${ARCHIVE_PLATFORM}.zip
          ./${ARCHIVE_PLATFORM}/bin/streamx --version | grep -q "${{ github.ref_name }}"

      - name: Test JAR archive from GitHub release
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          gh release download "${{ github.ref_name }}" \
            --repo "${{ needs.release.outputs.release_repo }}" \
            --pattern "streamx-jar-*.zip" \
            --dir .
          unzip streamx-jar-*.zip
          streamx-jar-*/bin/streamx --version | grep -q "${{ github.ref_name }}"